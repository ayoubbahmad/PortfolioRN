/*
 * ProjectsScreen Messages
 *
 * This contains all the text for the ProjectsScreen Screen.
 */

export const scope = 'app.screen.ProjectsScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ProjectsScreen Screen!',
    },
  },
};

export default messages;
