// import { selectProjectsScreenDomain } from '../selectors';

describe('selectProjectsScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
