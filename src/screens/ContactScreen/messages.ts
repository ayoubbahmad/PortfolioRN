/*
 * ContactScreen Messages
 *
 * This contains all the text for the ContactScreen Screen.
 */

export const scope = 'app.screen.ContactScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ContactScreen Screen!',
    },
  },
};

export default messages;
