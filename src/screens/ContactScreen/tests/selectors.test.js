// import { selectContactScreenDomain } from '../selectors';

describe('selectContactScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
