/*
 * Params Messages
 *
 * This contains all the text for the Params Screen.
 */

export const scope = 'app.screen.Params';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the Params Screen!',
    },
  },
  tile: {
    scope: `${scope}.title`,
    options: {
      defaultValue: 'This is the Params Screen!',
    },
  },
};

export default messages;
