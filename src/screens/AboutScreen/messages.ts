/*
 * AboutScreen Messages
 *
 * This contains all the text for the AboutScreen Screen.
 */

export const scope = 'app.screen.AboutScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the AboutScreen Screen!',
    },
  },
};

export default messages;
